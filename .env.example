# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง (ห้าม commit ไฟล์ .env ขึ้น git)

# LIFF ID จาก LINE Developers Console > ช่อง LIFF app ที่สร้าง
VITE_LIFF_ID=

# URL ของ backend API ที่รันบน Render เช่น https://your-app.onrender.com/api
VITE_API_BASE_URL=
